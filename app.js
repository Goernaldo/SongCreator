(function(){'use strict';window.AdminStatistics=Object.freeze({get:()=>{const s=AdminCore.snapshot(),users=s.users;return{users:users.length,active:users.filter(u=>!u.banned).length,premium:users.filter(u=>u.premium).length,banned:users.filter(u=>u.banned).length,songs:s.counters.songs||users.reduce((n,u)=>n+u.songs,0),covers:s.counters.covers||users.reduce((n,u)=>n+u.covers,0),packages:s.packages.length,popularThemes:s.activity.popularThemes,popularGenres:s.activity.popularGenres,lastLogins:s.activity.lastLogins};}});}());
