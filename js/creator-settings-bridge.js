(function(){'use strict';if(!window.SettingsStore)return;const settings=SettingsStore.load().creator||{},defaults={languages:settings.language,genres:settings.genre,tempo:settings.tempo};Object.entries(defaults).forEach(([category,value])=>{if(!value)return;const section=document.querySelector(`[data-category="${category}"]`);if(!section||section.querySelector('.option-card.active'))return;const button=[...section.querySelectorAll('.option-card')].find(item=>(item.dataset.value||'').localeCompare(value,'de',{sensitivity:'base'})===0);if(button&&!button.disabled)button.click()});}());
